server:
  port: 8002

spring:
  application:
    name: salon-service

  config:
    import: "optional:configserver:http://config.platform:8888"

  # cloud:
  #   gcp:
  #     credentials:
  #       location: classpath:gcp-key.json
  #     project-id: directed-post-506508-i4

gcp:
  storage:
    bucket.name: salon-bucket1